<li>
	{{burger_name}}

        <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}"> 
            {{#if devoured}}devour!!!{{else}}that was delicious, I'll have another!{{/if}}
        </button>

</li>